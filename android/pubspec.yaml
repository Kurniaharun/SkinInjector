name: skinject
description: SkinJECT — MLBB Skin Injector (Root & Shizuku) by KurrXd
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  http: ^1.2.2
  dio: ^5.7.0
  shared_preferences: ^2.3.3
  path_provider: ^2.1.5
  cached_network_image: ^3.4.1
  lucide_icons: ^0.257.0
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
